import fs from 'node:fs/promises';
import path from 'node:path';
const dir=path.join(nodeRepl.cwd,'docs/plans');
const read=n=>fs.readFile(path.join(dir,n),'utf8');
let p=(await read('THREE_STAGE_MOTION_PLAN.md')).replaceAll('\r\n','\n');
let a=(await read('STAGE_A_AGENT_INSTRUCTIONS.md')).replaceAll('\r\n','\n');
for(const [name,data] of [['THREE_STAGE_MOTION_PLAN_v1.1_review.md',p],['STAGE_A_AGENT_INSTRUCTIONS_v1.1_review.md',a]]) {
  try { await fs.writeFile(path.join(dir,name),data,{flag:'wx'}); }
  catch(e) {if(e.code!=='EEXIST') throw e; if(await read(name)!==data) throw Error('Archive already differs: '+name);}
}
try {await fs.copyFile(path.join(dir,'THREE_STAGE_MOTION_PLAN.docx'),path.join(dir,'THREE_STAGE_MOTION_PLAN_v1.0.docx'),1);} catch(e) {if(e.code!=='EEXIST') throw e;}
function replace(text,old,value){if(!text.includes(old))throw Error('Missing replacement: '+old.slice(0,100));return text.replace(old,value);}
function section(text,start,end,value){const i=text.indexOf(start),j=text.indexOf(end,i+start.length);if(i<0||j<0)throw Error('Missing section '+start);return text.slice(0,i)+value+'\n\n'+text.slice(j);}
p=replace(p,'Version 1.1','Version 1.2');
p=section(p,'### Changes from v1.0','## 1. Architecture',`### Review outcome and scope

Version 1.0 and the reviewed version 1.1 are preserved. Version 1.2 accepts the review's fixed-smoother, K=C, and continuous-feature controls; geometry-based initialization; efficient alternating optimization; and explicit numerical reporting. It defers the HMM, a second continuous-input contextualizer, subject classifiers, and BABEL execution. These additions would enlarge the architecture before the discrete pipeline is tested. REVIEW_RESPONSE_v1.2.md records each decision and the factual corrections.

The principal objective remains learning coarse action structure from fine discrete motion sequences. Stage A supplies local consistency, Stage B learns context from code sequences, and Stage C combines them. A1-KC is a targeted control, not a vocabulary sweep. A-cont is a separate reference, not a continuous-input replacement for the proposed architecture. K=500, both normalization conditions, and all three seeds remain fixed.`);
p=replace(p,'The only exceptions are the declared competing arms A-cont and B-hubert. They consume the same standardized PCA windows the tokenizer consumes, and are never combined with the code input.','The sole continuous-window comparison is A-cont in Stage A. It is never combined with the code input. Vocabulary prototype geometry is also used once for initialization; categorical fitting and inference subsequently receive code IDs and frame masses only. This is an explicit geometric initialization prior, not a claim that code IDs alone determine the initial model.');
p=replace(p,'beyond trivial smoothing, an HMM, a class-matched vocabulary, and the undiscretized features?','beyond a fixed smoother, a class-matched vocabulary, and a continuous-feature reference?');
p=replace(p,'(code input, and a continuous-input variant)','(code input only)');
p=section(p,'**Declared claim.**','The exp2round and Q1/Q2',`**Hypothesis, not assumed result.** A fixed fine vocabulary may support action segmentation when several codes can express one action and contextual evidence can change the assignment of a code. A1 versus A1-KC tests fine versus class-count-sized vocabularies under the declared pipeline. The same-checkpoint SMQ baseline is a system comparison: its tokenizer, objective, and temporal resolution also differ. It cannot by itself isolate the effect of K. The encoder was trained inside SMQ; choosing K independently of C does not make the full representation independent of the original class-count choice.

A-cont tests a normalized continuous representation with cosine emissions. It is a useful competitor, but categorical negative log-likelihood and cosine costs have different geometries. Neither winning nor losing this comparison establishes a universal causal benefit of discretization. If the code arm does not win, report that its segmentation advantage over this continuous competitor was not demonstrated. Reusability across action granularities remains a future hypothesis.

**Prior evidence, qualified by protocol.** In Q1/Q2, latent K=500 hard oracle-segment histograms at the planned windows gave F1@50 35.57 ± 6.86 on HuGaDB (W=15) and 6.19 ± 0.20 on LARa (W=12). K=10 hard histograms gave 48.33 ± 0.43 and 23.26 ± 2.57. These are warning signs for hard histogram clustering, not results for the proposed transport decoder. K=500 soft histograms at base temperature gave 61.90 ± 4.54 and 20.15 ± 2.59, so useful K=500 results were not confined to supervised readouts. All values are pooled oracle-boundary diagnostics, mean ± population SD across three seeds, not matched free-boundary baselines.

At these windows, I(U;S) exceeded I(U;A): 1.680 versus 1.428 nats on HuGaDB, and 1.812 versus 0.647 on LARa. Subject association merits monitoring. The inequality does not establish that shared codes mostly identify subjects, that subject variation caused the clustering results, or that all useful action information is absent. The targets have different marginal entropies, and codes may encode both. Sources: Q1/Q2 clustering_summary.csv and information_summary.csv, representation=latent, windows=15/12.`);
p=replace(p,'With K=500 it gives a cost range of roughly [0, 2], comparable to ASOT\'s cosine cost.','It is not a bounded cosine-like cost. With state count mass M_a and the stated smoothing, the maximum possible cost for an unobserved code is log(K*(M_a+eta)/eta)/log(K), which can exceed 2. Compare fitting cost distributions and objective components without tuning on action scores.');
p=replace(p,'Cluster the K prototypes (in the tokenizer\'s PCA space) into C groups g(u), using weighted KMeans with sample weights c_u, the vocabulary\'s stated KMeans settings, and the pipeline seed.','Cluster the occupied prototypes (in the tokenizer\'s PCA space) into C groups g(u), using weighted KMeans with sample weights c_u, the vocabulary\'s stated KMeans settings, and the pipeline seed.');
p=replace(p,'If fewer than C codes have c_u>0, mark the configuration unavailable.','If fewer than C distinct occupied prototypes exist, mark the configuration unavailable. At K=C with distinct occupied prototypes, set g(u)=u directly; this is the exact one-prototype-per-state grouping, without a redundant KMeans fit.');
p=section(p,'Rationale, declared before scoring:','### Explicit objective',`Rationale, declared before scoring: a one-second histogram has at most four contributing windows on HuGaDB and five on LARa, including boundary overlaps. It is sparse in 500 dimensions, but not necessarily near-one-hot. Prototype grouping uses the fitted vocabulary geometry, is invariant to permutation of recording order, and gives a shared initialization for A0/A1/A-cont. This is a defensible initialization choice, not a demonstrated repair of subject bias. Save the geometry dependency and grouping; do not add an initialization sweep.`);
p=replace(p,'| T | ASOT training defaults (alpha=0.3)','| T | Pinned CLI training defaults (alpha=0.3)');
p=replace(p,'| E | ASOT evaluation defaults (alpha=0.6)','| E | Pinned CLI evaluation defaults (alpha=0.6)');
p=replace(p,'The weaker E marginal (lambda=0.01) is more tolerant of recordings that contain only a few of the C actions. This is a declared reason for including E, not a prediction of its score.','E is a bundled decoding sensitivity: it changes four coefficients at once and does not isolate the marginal penalty. It does not refit the emissions, so it is not evidence about fitting the model under E. These values are src/train.py command-line defaults at the pinned commit; constructor defaults and run_bf.sh overrides differ. They are not universally validated ASOT settings for these categorical costs. Compute primary T predictions before E. Always enumerate and report missing E results rather than selecting settings by score.');
p=replace(p,'Implement the solver in torch float64. Run production solves on CUDA when available (the project workstation has an RTX 4090), otherwise on CPU. Record the device per cell. Batch recordings with padding and masks. Keep a per-recording step size and per-recording stopping state, and implement the temporal term with 1-D convolution, never a dense L-by-L matrix. Parity and finite-difference tests run in float64 on CPU. Cap CPU numerical threads at eight.','Implement in torch float64 with CPU as the default. An explicit CUDA launch may use the existing GPU after numerical parity and a label-blind timing probe; do not assume float64 GPU speedup. Freeze the device for a run and never silently switch on failure. Implement correctness per recording first, then optional bounded batches with masks and per-recording steps/stopping. Kernel amplitude must use each recording\'s true L, not padded batch length. Implement the temporal term with convolution or prefix sums, never a dense L-by-L matrix. CPU parity/finite-difference tests precede acceleration. Cap CPU numerical threads at min(8, logical CPU count).');
p=p.replaceAll('generalized EM','inexact alternating minimization').replaceAll('generalized-EM','inexact alternating');
p=replace(p,'Each accepted step decreases F_r, so every outer iteration decreases the complete objective.','Each accepted step is nonincreasing within a documented floating-point tolerance; the exact emission update must also be nonincreasing. Verify the complete weighted objective, including its prior. This is inexact block-coordinate optimization, not ordinary likelihood EM.');
p=replace(p,'- *converged*: all stopping rules met.','- *converged*: the declared outer objective-change and final stationarity criteria are met; this is operational convergence, not a global-optimum certificate.');
p=replace(p,'- *capped*: an iteration cap was reached, with a finite, nonincreasing accepted-objective trace. Capped cells stay in the primary aggregate, flagged and counted.','- *capped*: the outer or final-inference cap was reached, with finite feasible iterates and a nonincreasing accepted-objective trace. Include these in a predeclared budgeted-algorithm table, with status counts and residuals visible. Also show convergence-qualified paired comparisons restricted to the same seeds; never present that selected subset as the full-grid result. The planned 25-step inner schedule is not itself a cap failure. An interrupted cell is not capped.');
p=replace(p,'- *invalid*: a nonfinite value, a backtracking failure, or an objective increase. Invalid cells are excluded and listed.','- *invalid*: nonfinite values, infeasibility, backtracking failure, or an objective increase exceeding numerical tolerance. Exclude the affected metric from valid aggregates but retain every planned cell and reason. Keep fitting and final-inference status separate for T and E; an E failure cannot invalidate a completed T result.');
p=replace(p,'| HMM | K=500 codes; categorical | sticky fixed transitions | Baum–Welch on theta | Does transport beat the canonical discrete-sequence model? |\n','');
p=section(p,'**HMM.**','**A1-KC.**',`**Deferred HMM.** A fixed sticky HMM is a worthwhile later sequence-model competitor, but adds a separate objective, implementation, and validation path. It is not part of this Stage A brief. Beating A0+filter will support a comparison with that particular smoother, not superiority over HMMs or all temporal methods. The review's proposed HMM also needs an explicit prior scale consistent with n_t/W likelihood weights and a geometric-duration interpretation before implementation.`);
p=replace(p,'With K=C, each prototype forms its own group.','With K=C, distinct occupied prototypes map directly to states. The log(K) scaling and per-code smoothing change with K, so this is the declared vocabulary-size protocol, not a perfectly isolated intervention holding every effective regularizer fixed.');
p=replace(p,'so the check is expected to fall near chance.','so it cannot isolate temporal modeling. Recording-level action/subject composition remains, and no near-chance outcome is guaranteed.');
p=section(p,'**Matrix.**','### Acceptance and interpretation',`**Matrix and scope.** There are four fitted arms: A0, A1, A1-KC, and A-cont. The derived A0+filter requires no fit. Pooled: 2 datasets × 2 normalization conditions × 3 seeds × 4 arms = 48 fits, plus four seed-111 permutation sanity fits = 52. Subject-disjoint: four folds × 48 = 192 fits. Total planned Stage A fits: 244. Each regular configuration yields ten prediction sets (four fitted arms plus filter, each under T and E); each permutation fit yields two. Keep fit IDs distinct from prediction-set IDs. Build shared tokenizer/preprocessing/grouping artifacts once. Score SMQ separately; compatible HiST-VQ/MASQ exports are optional matched system comparisons, not prerequisites.

**Order and decision point.** Run pooled T first, then the pooled sanity checks and E sensitivity, alternating datasets and completing three paired seeds per configuration. The runner stops after this protocol. The user may launch subject-disjoint evaluation separately with the same frozen settings and the pooled run ID. This decision manages scope and budget; it does not authorize changing settings, dropping losing arms, or claiming transfer without completing the folds. If transfer is not run, list it as not run and restrict conclusions to pooled fitting.

The launcher receives an explicit runtime budget, reserves 30 minutes for reporting, and records all unstarted/interrupted cells. Dry run lists the matrix and estimates from existing timings without fitting. An explicit timing-probe mode may run small label-blind jobs. Primary T work has priority over E; missing E cells remain visible. Never shrink the declared primary grid by score.`);
p=replace(p,'- the HMM forward–backward, checked against brute-force enumeration on tiny sequences\n','');
p=replace(p,'2. **The temporal mechanism:** A1 against A0+filter and the HMM. A0 is reported, but because it has no temporal term, beating it alone is not evidence for transport.','2. **Temporal assignment:** A1 against A0 identifies the effect of adding the declared structural term under paired fitting. A1 against A0+filter asks whether it improves on this fixed smoother. Neither proves superiority over all sequence models.');
p=section(p,'Freeze each Stage A K=500 tokenizer. Train two contextualizers','Both use position information',`Freeze each Stage A K=500 tokenizer. Train one contextualizer, B-code, whose input is code IDs and a learned mask token. A continuous-input masked-prediction competitor is deferred to a separately specified follow-up; it is not another primary architecture in this plan.`);
p=replace(p,'Both use position information','Use position information');
p=replace(p,'Replace every selected input position with the mask token or vector.','Replace every selected input position with the mask token.');
p=replace(p,'B-hubert is closer, but it still uses frozen K-means targets from a frozen encoder, and there are no target-refinement iterations. ','There are no target-refinement iterations. ');
p=replace(p,'HuGaDB has roughly 76k tokens in total, so expect early stopping to act quickly; this is not a reason to change the architecture after seeing scores.','HuGaDB\'s existing W=15 cache contains approximately 76k tokens; overlapping training chunks do not create independent observations. Do not infer convergence speed or adequacy of training data from that count alone.');
p=replace(p,'Reuse one learned contextualizer per variant','Reuse one learned contextualizer');
p=section(p,'**Subject-shortcut diagnostics.**','### Stage B checkpoint',`**Subject-association diagnostics.** Keep diagnostics descriptive and inexpensive. Save frame-weighted subject-by-predicted-state counts and mutual information at evaluation. For contextual embeddings, sample at most 10,000 complete windows uniformly with seed 111, including recording/subject IDs. Report cosine 10-nearest-neighbor same-recording/same-subject rates before and after excluding same-recording candidates, exclude self-neighbors, and report the corresponding sampling-composition reference rates. This exposes local repetition and cross-recording association separately. Subject metadata are labels for these diagnostics, even though action annotations are not used. Do not use them to select checkpoints or settings. A fitted subject classifier is deferred; it would add split and optimization choices. None of these diagnostics isolates subject identity from action/recording conditions.

The internal validation split excludes recordings from contextualizer gradient updates, but the frozen tokenizer may already have used them. Describe it as contextualizer validation, not an independently held-out tokenizer evaluation.`);
p=replace(p,'deterministic reload inference, and B-hubert\'s input projection and masking','deterministic reload inference');
p=replace(p,'Freeze the Stage B checkpoints.','Freeze the Stage B checkpoint for each configuration.');
p=replace(p,'| B-hubert context | Context-only, beta=0 | Context-only + filter | Contextual ASOT |\n','');
p=replace(p,'for each contextualizer by permuting complete code windows (and, for B-hubert, the matching PCA windows)','for B-code by permuting complete code windows');
p=replace(p,'Add a declared order-destruction diagnostic for B-code','Add a pooled, seed-111-only order-destruction diagnostic for B-code');
p=section(p,'**When the declared claim is supported.**','**Where the field stands.**',`**What each comparison can establish.** Stage A can test A1 versus A1-KC under a shared decoder family. Stage C tests adding context at K=500 and adding transport to that context. Because there is no K=C contextualizer, beating A1-KC with Stage C cannot isolate vocabulary size: context also changed. Preserve this boundary rather than adding another contextualizer grid.

Report F1@50 paired differences and all five metrics under primary T, across the declared seeds and both datasets. Show E directions separately. Restrict transfer claims to completed subject-disjoint runs. A strong positive result requires agreement across the relevant controls and no concealed collapse or numerical failure; it is not a pre-written publication conclusion. A-cont matching or exceeding A1 means no segmentation advantage over this continuous competitor was demonstrated, not that discretization has no possible value.`);
p=replace(p,'All three papers also evaluate BABEL, so BABEL is required before any publication claim.','These papers evaluate BABEL, making it a natural later benchmark for comparable positioning. BABEL is outside Stages A–C and is not a universal prerequisite for publishing a narrower, properly supported result. Do not advertise state of the art from unmatched paper-table numbers.');
p=replace(p,'categorical/continuous/HMM models','categorical/continuous models');
p=replace(p,'A dry run prints the full grid, verifies caches, runs a short timing probe, and estimates cost without fitting.','A dry run prints the full grid and verifies caches without fitting. A separate explicit timing probe estimates cost using tiny label-blind jobs.');
p=replace(p,'Stage A may use the existing CUDA GPU through torch and falls back to CPU; request no new hardware.','Stage A defaults to CPU; CUDA is an explicit verified run option, with no silent fallback or new hardware request.');
p=replace(p,'- convergence (converged/capped/invalid) and coverage','- budgeted-algorithm results, convergence-qualified paired comparisons, status counts, and coverage');
p=replace(p,'(version 1.1)','(version 1.2)');
p=replace(p,'and the training/evaluation coefficient lineages.','and the temporal structural prior.');
p=replace(p,'coefficient defaults: src/train.py and run_bf.sh.','coefficient lineage: src/train.py CLI defaults; constructor defaults and run_bf.sh use different values.');
p=replace(p,'Source of masked hidden-unit prediction; B-hubert adopts its continuous-input/discrete-target structure without target refinement.','Source of masked hidden-unit prediction. The planned discrete-input B-code model is an adaptation, not a HuBERT reproduction.');
p=replace(p,'Closest prior fine-to-coarse design; unmatched published reference.','Prior fine-to-coarse design; unmatched published reference. https://arxiv.org/html/2604.15196v1');
p=replace(p,'Current published state of the art on HuGaDB and LARa; unmatched published reference.','Recent reported benchmark results; unmatched published reference. https://arxiv.org/html/2608.29891v1');

// Bring the operational brief into the same version and scope.
a=a.replaceAll('version 1.1','version 1.2');
a=replace(a,'Version 1.0 of these instructions is preserved as `STAGE_A_AGENT_INSTRUCTIONS_v1.0.md`.','Versions 1.0 and the reviewed 1.1 are preserved. See `REVIEW_RESPONSE_v1.2.md` for accepted, corrected, and deferred changes.');
a=replace(a,'- **Allowed:** the sticky categorical HMM specified in §4, which is a comparison arm.','- HMMs and continuous-input contextualizers are deferred. Do not implement them in Stage A.');
a=replace(a,'  hmm.py             # sticky categorical HMM (fixed transitions, Baum–Welch on theta)\n','');
a=replace(a,'- `hmm`: K=500 codes, sticky fixed transitions (§4).\n','');
a=replace(a,'The HMM has no setting and yields one prediction set. Thus each configuration yields 11 scored prediction sets.','There are four fitted arms plus the derived filter; each configuration yields ten prediction sets. T is primary; E is a bundled sensitivity on fixed T-fitted parameters, not an E-trained model or an isolated marginal-penalty test. These settings come from the pinned src/train.py CLI defaults, not the constructor or run_bf.sh overrides. Compute T first; list E jobs not reached within the budget.');
a=replace(a,'3 seeds × 5 fitted arms = 60, plus 4 permutation sanity fits = **64 fits**','3 seeds × 4 fitted arms = 48, plus 4 permutation sanity fits = **52 fits**');
a=replace(a,'× 4 folds = **240 fits**','× 4 folds = **192 fits** (244 total planned fits)');
a=replace(a,'2. Fit C-cluster KMeans on the K prototypes in PCA space with `sample_weight=c`, using the vocabulary KMeans settings and the pipeline seed.','2. Fit C-cluster KMeans on the occupied prototypes in PCA space with `sample_weight=c`, using the vocabulary KMeans settings and the pipeline seed. At K=C with distinct occupied prototypes, assign `g[u]=u` directly instead.');
a=replace(a,'If fewer than C codes have `c[u]>0`, the configuration is unavailable; do not change C. For K=C, the grouping reduces to one prototype per group; verify this rather than assume it.','If fewer than C distinct occupied prototypes exist, the configuration is unavailable; do not change C. At K=C, validate the direct one-prototype-per-state grouping.');
a=replace(a,'A0, A1 and the HMM share `theta0`.','A0 and A1 share `theta0`. Geometry is a declared initialization input; categorical fitting/inference thereafter consume only code IDs and masses. This is not a geometry-free initialization.');
a=replace(a,'Use the fixed `log(K)` scale for every recording and outer iteration. Do not refit a cost normalizer to current costs.','Use the fixed `log(K)` scale for every recording and outer iteration. Do not refit a cost normalizer to current costs. This cost is not bounded by 2. Save fitting cost quantiles and objective components. The K=C arm changes log(K) and per-code smoothing; disclose that effective regularization is not perfectly matched across K.');
a=section(a,'### Sticky categorical HMM','### Mode filter',`### Deferred model family

Do not implement the review's sticky HMM. It would add a separate likelihood and implicit geometric-duration prior. A0+filter is the bounded temporal comparator here; conclusions must name that particular smoother, not all temporal models.`);
a=replace(a,'Run on CUDA if available (record the device) and fall back to CPU. Batch recordings with padding and masks, keeping a per-recording step size and stopping state.','Default to CPU. CUDA requires an explicit launch option, successful numerical checks, and a label-blind timing probe. Record/freeze the selected device per run; no silent fallback. Implement per-recording correctness first. If batching, use bounded padding/masks, per-recording step/stopping, and each true recording length L in the kernel amplitude; padded length must not change its objective.');
a=a.replaceAll('generalized EM','inexact alternating minimization').replaceAll('generalized-EM','inexact alternating');
a=replace(a,'halve it on an increase in that recording\'s F, and allow at most 30 backtracks.','halve it on an increase in that recording\'s F exceeding `1e-12*max(1,abs(F_old))`, and allow at most 30 backtracks.');
a=replace(a,'Save the stopping reason and actual residuals, not only a boolean.','Save the stopping reason and actual residuals, not only a boolean. Define the row-centered residual as `max(abs(grad - grad.mean(axis=1,keepdims=True)))` over valid entries only. Report maximum and frame-weighted median residual across recordings. This is inexact block-coordinate optimization; the 25-step inner schedule is planned, not an optimization-failure status.');
a=replace(a,'- `converged`: all stopping rules met.','- `converged`: the declared outer and final stopping rules met; no global-optimum claim.');
a=replace(a,'- `capped`: an iteration cap was reached, with a finite, nonincreasing accepted-objective trace. Included in the primary aggregate, flagged, and counted.','- `capped`: outer or final-inference cap reached with feasible, finite iterates and a nonincreasing accepted-objective trace. Include in the explicitly labelled budgeted-algorithm table with status counts/residuals. Also report convergence-qualified comparisons on the same paired seeds, without promoting that selected subset to the main result. Deadline-interrupted jobs are incomplete, not capped.');
a=replace(a,'- `invalid`: a nonfinite value, a backtracking failure, or an objective increase. Excluded and listed.','- `invalid`: nonfinite values, infeasibility, backtracking failure, or an objective increase exceeding tolerance. Exclude invalid metrics and retain the cell/reason. Record fitting status and T/E inference status separately so E cannot invalidate T.');
a=replace(a,'CPU and CUDA agreement to 1e-10 when CUDA exists','CPU and CUDA agreement on small fixed cases at atol=1e-8, rtol=1e-6 when explicitly enabled; verify objective/gradients and posterior probabilities, and flag argmax ties rather than demanding identical hard labels there');
a=replace(a,'6. HMM: forward–backward marginals and log-likelihood against brute-force enumeration on tiny sequences; a nondecreasing log-posterior over Baum–Welch iterations; transitions unchanged; terminal-window scaling.\n','');
a=a.replace('7. Mode filter:','6. Mode filter:').replace('8. Null:','7. Null:').replace('9. Evaluation:','8. Evaluation:').replace('10. Operations:','9. Operations:');
a=replace(a,'theta/mu, HMM transitions, permutations','theta/mu, permutations');
a=replace(a,'cost scale, HMM self-transition, true frame counts','cost scale, true frame counts');
a=replace(a,'As a label-free diagnostic, also save the subject-state mutual information (subject IDs are metadata) to expose subject-aligned states.','As an action-annotation-free diagnostic, save frame-weighted subject-by-state counts and subject-state mutual information. Subject IDs are labels for this diagnostic; do not interpret the association as a causal subject-identity effect.');
a=replace(a,'2. **The temporal mechanism:** A1 against A0+filter and against the HMM, with A0 shown.','2. **Temporal assignment:** A1 against A0 and A0+filter; superiority is only over the specified smoother.');
a=replace(a,'- paired seed differences, and means/population SD across exactly three complete seeds','- paired seed differences and means/population SD across exactly three completed seeds, with capped counts visible in the budgeted table; convergence-qualified paired subsets are secondary and list their actual seed counts');
a=replace(a,'- the HMM is a fixed-transition comparison, not a duration model','- A-cont differs in emission geometry, K=C differs in effective scaling/smoothing, and SMQ is a matched-population system comparison, not an isolated K intervention');
a=replace(a,'`-RunId`, `-Resume`, and optional `-Device auto|cpu|cuda` (default auto).','`-RunId`, `-Resume`, `-TimingProbe`, and optional `-Device cpu|cuda` (default cpu).');
a=replace(a,'- Dry run lists planned cells and cache status, runs a short timing probe per arm, and projects cost without fitting the grid.','- Dry run lists planned cells/cache status and projects from existing timing records without fitting. `-TimingProbe` is a separate explicit mode that runs tiny label-blind jobs; it never launches the grid or evaluates action scores.');
a=replace(a,'- Set `OPENBLAS_NUM_THREADS` and `OMP_NUM_THREADS` to 8 before Python starts. Unset thread caps have caused segfaults on this machine.','- Set numerical-library thread caps to min(8, logical CPU count), including `OPENBLAS_NUM_THREADS`, `MKL_NUM_THREADS`, and `OMP_NUM_THREADS`, before Python starts.');
a=replace(a,'There is no `all` option and no automatic numerical gate.','There is no `all` option and no automatic numerical gate. This is a budget/scope decision: reuse the frozen settings and all primary arms if continued, otherwise mark transfer not run. A pooled-score inspection does not authorize tuning before claiming generalization.');
a=replace(a,'- numerical, parity and HMM test results','- numerical, parity, filter, and continuous-update test results');
a=replace(a,'- measured timing-probe costs and the projected pooled runtime','- measured timing-probe costs if explicitly run, otherwise the projection\'s missing timing evidence');
await fs.writeFile(path.join(dir,'THREE_STAGE_MOTION_PLAN.md'),p);
await fs.writeFile(path.join(dir,'STAGE_A_AGENT_INSTRUCTIONS.md'),a);
nodeRepl.write({planWords:p.split(/\s+/).length,instructionWords:a.split(/\s+/).length,version:'1.2'});
